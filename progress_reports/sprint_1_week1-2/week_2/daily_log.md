# Daily Log — Tuần 2 (23/07 – 29/07/2026)
| Ngày | Thứ | Công việc thực hiện | Kết quả / Output | Vướng mắc | Kế hoạch ngày tiếp theo |
|---|---|---|---|---|---|
| 23/07/2026 | Thứ 5 | Rà soát pipeline sau Sprint 1, kiểm tra các leakage columns theo data dictionary, bổ sung các cột còn thiếu và thêm lớp kiểm tra `assert_no_leakage()`. | Mở rộng `LEAKAGE_COLUMNS` từ 13 lên 38 cột; bổ sung `assert_no_leakage()` trong `src/data/filter_vintage.py`.                                                               | Phát hiện nhiều biến tưởng là thông tin tại thời điểm cấp khoản vay nhưng thực chất được cập nhật sau giải ngân, đặc biệt nhóm FICO. | Tiếp tục rà soát multicollinearity giữa các biến FICO và đánh giá phương án xử lý.                                    |
| 24/07/2026 | Thứ 6 | Phân tích multicollinearity giữa `fico_range_low` và `fico_range_high`; thực hiện thực nghiệm để đánh giá phương án gộp biến.                           | Tạo biến `fico_mid` thay thế hai biến FICO gốc; cập nhật `src/features/clean.py`; hoàn thành `experiments/exp01_fico_mid.py`.                                               | Hai biến FICO có tương quan rất cao làm hệ số Logistic Regression bị chia nhỏ, gây khó diễn giải scorecard.                          | Tiếp tục kiểm tra nguyên nhân hệ số `revol_util` có dấu bất thường.                                                   |
| 27/07/2026 | Thứ 2 | Điều tra nguyên nhân hệ số `revol_util` sai dấu; kiểm tra nhiều giả thuyết và rà soát notebook xây dựng scorecard.                                      | Xác định nguyên nhân là suppressor effect với FICO; sửa ô kiểm tra dấu hệ số trong Notebook 04; hoàn thành `experiments/exp02_revol_util_sign.py`.                          | Hai giả thuyết ban đầu không đúng nên mất thời gian kiểm chứng trước khi tìm được nguyên nhân thực sự.                               | Rà soát lại quy trình feature selection và time-based split để tránh data leakage gián tiếp.                          |
| 28/07/2026 | Thứ 3 | Kiểm tra lại thứ tự pipeline; chuyển bước chọn biến (IV) xuống sau time-based split và chạy lại feature selection.                                      | Quy trình chọn biến chỉ sử dụng dữ liệu train; kết quả lựa chọn thay đổi ở 3/16 biến so với trước.                                                                          | Việc thay đổi pipeline làm kết quả mô hình khác trước nên cần chạy lại toàn bộ pipeline để đảm bảo tính nhất quán.                   | Chạy lại toàn bộ notebook 02→05 và cập nhật các báo cáo theo kết quả mới.                                             |
| 29/07/2026 | Thứ 4 | Chạy lại toàn bộ pipeline sau khi hoàn thành các chỉnh sửa; cập nhật tài liệu và tổng hợp kết quả Sprint 1.                                             | Hoàn thành chạy lại notebook 02→05; cập nhật `reports/BRD.md`, `reports/eda_risk_report.md`; Logistic Regression + WOE đạt AUC test **0.6516**, toàn bộ 8/8 hệ số đúng dấu. | AUC giảm nhẹ so với trước nhưng phản ánh mô hình đáng tin cậy hơn sau khi loại bỏ leakage và chuẩn hóa pipeline.                     | Chuẩn bị mở rộng feature set (bureau features), tạo thêm feature engineering và huấn luyện lại LightGBM trong tuần 3. |
